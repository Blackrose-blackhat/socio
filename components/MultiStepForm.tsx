"use client";
import React from "react";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "./ui/card";

const MultiStepForm = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Lets get you onboard</CardTitle>
        <CardDescription>Follow the steps</CardDescription>
      </CardHeader>
      <CardContent></CardContent>
    </Card>
  );
};

export default MultiStepForm;
